# NITCC Backend — Shared + Gateway Dependencies

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
motor>=3.3.0                  # Async MongoDB driver
pymongo>=4.6.0

# Cache / Pub-sub
redis[asyncio]>=5.0.0

# Kafka
aiokafka>=0.10.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pyotp>=2.9.0                  # TOTP / RFC 6238

# HTTP Client
httpx>=0.26.0

# Rate limiting
slowapi>=0.1.9

# Metrics
prometheus-client>=0.19.0

# Structured logging
structlog>=24.1.0

# AI / LLM (optional — set provider in .env)
openai>=1.10.0
google-generativeai>=0.3.2
anthropic>=0.15.0
langgraph>=0.0.30
langchain>=0.1.0

# ML
tensorflow>=2.15.0            # Track failure model
scikit-learn>=1.3.0           # Data processing
numpy>=1.24.0
pandas>=2.0.0
mlflow>=2.9.0                 # Experiment tracking

# Scheduling / Pipeline
apache-airflow>=2.8.0

# Document generation
reportlab>=4.0.0              # Work order PDF export

# XML parsing (IMD weather feed)
lxml>=5.0.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6       # File uploads
aiofiles>=23.2.0

# Development / Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.26.0                 # Test client
faker>=22.0.0                 # Seed data generation
black>=23.12.0
ruff>=0.1.14
mypy>=1.8.0
